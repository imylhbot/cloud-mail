{
  "version": "5",
  "base": "cloud-mail-push-center-v4",
  "changes": [
    "batch import moved from sidebar dialog to /batch-users full page",
    "sidebar now only navigates to batch page",
    "global Mail API key made explicit in UI",
    "admin reveal/copy endpoint for complete global API key",
    "same global key works for every mailbox"
  ],
  "new_route": "/batch-users",
  "global_api_key": {
    "storage": "Cloudflare KV PUSH_CONFIG_V1.api.key",
    "scope": "all mailboxes",
    "per_user_key_required": false
  }
}