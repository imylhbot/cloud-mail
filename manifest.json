{
  "version": "3",
  "features": [
    "internal Hono mail API tester via app.request",
    "external HTTP tester retained",
    "unread extraction API",
    "max 10 unread per extraction",
    "optional mark-as-read after extraction",
    "atomic unread claim using D1 UPDATE...RETURNING",
    "no D1 migration"
  ],
  "unread_endpoints": [
    "GET /api/mail-api/unread/:address",
    "POST /api/mail-api/unread/:address"
  ]
}